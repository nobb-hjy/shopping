/**app.wxss**/
@import './style/iconfont.wxss';
/* 小程序不能写通配符 *，初始化样式手写标签 */
page,view,text,swiper-item,image,navigator{
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}
/* 主题颜色 */
page{
  /* 定义主题颜色 */
  --themeColor:#eb4450;
  /* 统一字体大小 */
  --fontSize:28rpx;
}